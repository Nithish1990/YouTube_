package application.controllers.mediapagecontrollers;

public interface MediaPageController {
    void renderPage(String URL);// the url will be video url in watchPageController and in channelPage controller channel url
}
